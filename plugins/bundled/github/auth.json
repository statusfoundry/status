{
  "type": "bearer-token",
  "provider": "github",
  "applicationId": "status-foundry.github",
  "fields": [
    {
      "key": "token",
      "label": "Personal access token",
      "type": "secret",
      "placeholder": "github_pat_...",
      "help": "Create a fine-grained GitHub token for this repository with read-only Metadata, Actions, Pull requests, and Issues access. Status stores it in Keychain.",
      "required": true
    }
  ]
}
