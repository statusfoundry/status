{
  "title": "Repository to watch",
  "description": "Configure one GitHub repository for read-only workflow and activity checks. Use Owner for the GitHub user or organization, and Repository for the repository name only.",
  "fields": [
    {
      "key": "owner",
      "label": "Owner",
      "type": "text",
      "placeholder": "statusfoundry",
      "help": "GitHub user or organization, for example statusfoundry.",
      "required": true
    },
    {
      "key": "repo",
      "label": "Repository",
      "type": "text",
      "placeholder": "status",
      "help": "Repository name only, for example status. Do not paste the full GitHub URL.",
      "required": true
    }
  ]
}
